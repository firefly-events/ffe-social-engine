{
  "name": "@ffe/db",
  "version": "0.1.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "build": "prisma generate",
    "test": "echo \"No tests\"",
    "lint": "eslint .",
    "db:migrate": "prisma migrate dev",
    "db:seed": "prisma db seed"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0",
    "eslint": "^8.0.0"
  },
  "prisma": {
    "seed": "node seed.js"
  }
}
